interface:
  display_name: "Zadar"
  short_description: "Repo-specific patterns and workflows for zadar"
  default_prompt: "Use the zadar repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true